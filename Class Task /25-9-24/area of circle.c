#include<stdio.h>
//area of circle
int main()
{	
	float radius; // data variable or defined variable radius as float..
	
	printf("Enter radius : "); // asked for input...
	scanf("%f", &radius); // imported the input to radius in data type float..
	
	printf("radius of the circle is :%f", 3.14 * radius * radius ); // radius of circle is pier r(sqaure)... 
	return 0;
}
